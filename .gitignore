venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
dist/
build/
.DS_Store
# Raw wiki/API fetches — large intermediates, re-fetchable from each dataset's _provenance.source_urls.
# Ignore the raw DATA, but KEEP the extraction/builder scripts that live alongside it (reproducibility).
data/raw/**
!data/raw/**/
!data/raw/**/*.py
